# 20260520

## by a human

claudeの５時間制限にひっかかって久しぶりにcodexに戻ってきた。まず現状を共有するところから。初期に痛手をくらったから、codexへの信頼感が薄い。とりあえず簡単なところから試していこう。しばらく時間がたってclaudeが使えるようになったので、戻る。codexはleanのコードをあまりトレインに入れてないのではと疑いたくなる。

anthropic公式のralph-loopを試し始めた。ある程度、同等のプロンプトを再投下するタイミングになったらこれを回すのがいいのかな。いったんralph-loopを試している間に、SmallGroupsかOddOrderTheoremの準備を始める。

## by claude (Tier C: Petersen / Hoffman-Singleton)

E (Feit-Thompson 不要化) の後、C (Petersen / HS 構造) を進めた。2 commits:

**`600676a`**: コア 4 新規ファイル
- `PetersenGraph.lean`: GP(5,2) 形 explicit 構築 → `IsSRGWith 10 3 0 1` by `decide`
- `SRGPredicates.lean`: `IsPetersenLike` / `IsHoffmanSingletonLike` abbrev
- `PetersenFixedData.lean`: 10 fixed-point Petersen 構造 + 3 個の bridge
  (`induced_degree_three`, `autFixedNeighborFinset_card_eq_three`,
   `petersenFixedData_complement_neighbor_count` = 54)
- `HSFixedData.lean`: 50 fixed-point HS 構造 (Set.ncard 形) + 同等 bridge
  (`induced_degree_seven`, `autFixedNeighborFinset_card_eq_seven`,
   `hsFixedData_complement_neighbor_count` = 50)

**`187f4a0`**: Lem 17/18 wrappers — `lem17_case1_complement_count_eq_54` +
`lem17_case1_orderOf_dvd_27_with_petersenFixedData` (conditional bridge:
PetersenFixedData + `orderOf σ ∣ 54` ⟹ `orderOf σ ∣ 27`), Lem 18 同等。

Lake build clean (4040 jobs, was 4037 before)。

### つまづき memo

- `native_decide` は mathlib linter 禁止 (kernel-trust 理由)。`decide` で
  通すには `fin_cases v <;> decide` + 適切な absurd handling が必要 (100 cases)。
- `SimpleGraph` の `loopless : Std.Irrefl Adj` は `⟨fun u h => ...⟩` で wrap。
- `IsSRGWith` の field projections (`.regular`, `.card`) は `DecidableRel G.Adj`
  + `Fintype V` 前提 → `IsPetersenLike` は `abbrev` でないと projection が
  動かない (def だと再帰扱いで termination 検査 fail)。
- `Set.ncard ↔ Finset.card` bridge は `Set.ncard_eq_toFinset_card'` +
  `Set.toFinset_setOf` の二段階。

### 残務 (Tier C)

- **C3.4** semi-regular orbit argument (deferred-heavy): `⟨σ⟩` の
  `N(a) \ Fix(σ)` 上の作用 semi-regular から `orderOf σ ∣ |N(a) \ Fix(σ)|`。
- **C2.1** HS explicit 50-vertex construction (重い)。
- **C1.2b** `Aut(Petersen) ≅ S₅` uniqueness。

## by claude (Tier C 拡張: 全 FixedData bridges)

Stop hook が「もっと簡単なものがあるはず」と指摘したので、Tier C を拡張。
2 commits 追加:

**`cb8e192`**: C5/K155/Singleton/Empty FixedData bridges
- `C5FixedData`: induced_degree_two, autFixedNeighborFinset = 2,
  complement = 55
- `K155FixedData`: center bridge (= 55) / leaf bridge (= 1) /
  complement (center = 2, leaf = 56)
- 新規 `SingletonFixedData` (Fix = {v}) と `EmptyFixedData` (Fix = ∅)
- Lem 18/19 case (1)(2)(3) conditional wrappers

**`e92edcc`**: 全 FixedData に `fixedVertexCount_eq_N` 追加 + Lem 4 wrappers
- Petersen 10, HS 50, C5 5, K155 56 (1 center + 55 leaves, disjoint),
  Singleton 1, Empty 0
- `lem4_case{1,2,4,5,6}_with_*FixedData` で各 case を FixedData
  parameterise

これで Lem 4 の 6-case classification の case (3) star (`2 + 7l`
vertices) 以外、すべて FixedData → result の直接 bridge ができた。
case (3) は K_{1,55} と異なる general star なので skip。

### 観察: pattern としての FixedData → 既存 case lemma

各 FixedData は (1) `fixedVertexCount_eq_*` と (2) `autFixedNeighborFinset_card_eq_*`
を持つ。既存の case lemma はこれらを hypothesis として取るので、FixedData
渡せばそのまま wrap できる。これで「具体的な fix-shape data ⟹ 抽象的な
arithmetic conclusion」の直接 bridge が完成した。

残るのは semi-regular orbit argument (C3.4) で、これはが orderOf σ ∣
|N(a) \ Fix(σ)| を結ぶ部分。これは deferred-heavy。

## by claude (Tier C 追加: Petersen graph 性質拡張)

ロードマップから「Petersen graph の続き」として、Petersen 本体と PetersenFixedData
の追加性質を埋めた。`Aut(Petersen) ≅ S₅` は 10! 規模で重いので保留し、より
直接 useful な girth/edge-count 系の bridge を優先。

### 追加した補題

**`PetersenGraph.lean`** (explicit `SimpleGraph (Fin 10)` 側):
- `petersenGraph_edgeFinset_card = 15` — handshaking
  (`sum_degrees_eq_twice_card_edges`) 経由で `10 · 3 / 2`
- `petersenGraph_triangleFree` — `λ = 0` から直接
- `petersenGraph_no_C4` — `λ = 0 ∨ μ = 1` の case 分け。case 2 では
  `card commonNeighbors ≤ 1` vs 「`b ≠ d` の 2 元」で矛盾

**`SRGPredicates.lean`** (abstract `IsPetersenLike` / `IsHoffmanSingletonLike` 側):
- `isPetersenLike_edgeFinset_card = 15` (10 · 3 / 2)
- `isHoffmanSingletonLike_edgeFinset_card = 175` (50 · 7 / 2)
- 一般 SRG の handshaking 形式の特殊化として、`IsSRGWith n k _ _` →
  `|E| = n · k / 2` を Petersen / HS instance で書き下した形

**`PetersenFixedData.lean`** (σ-fixed Γ-subgraph 側):
- `induced_triangleFree` / `induced_no_C4` — `induced_adj_iff` で
  `petersenGraph` の対応補題に reduce
- `induced_adj_pairs_card_eq_30` — 100 個の順序付き Fin 10 × Fin 10 pair
  のうち Γ-adjacent なのは 30 個 (= 2 · 15 edges)。証明は filter_congr で
  Petersen.Adj に bridge した上で `decide`

**`HSFixedData.lean`** (σ-fixed Γ-HS-subgraph 側):
- `induced_triangleFree` — `induced_lambda` (`λ = 0`) +
  `Set.ncard_eq_zero` で「空集合への矛盾」を取る
- `induced_no_C4` — `induced_mu` (`μ = 1`) + 「`{j, l}` の 2-element
  subset」が common neighbour 集合に含まれることから `ncard ≥ 2 > 1` で矛盾

### 設計上の選択

- `IsPetersenLike` / `IsHoffmanSingletonLike` を `abbrev` (not `def`) に
  していたので、`.regular` / `.card` projection がそのまま通る。これで
  handshaking lemma の適用が `simp [hreg]` ベースで済む。
- HSFixedData 側は explicit graph がないので、`λ`/`μ` 形式の `Set.ncard`
  field から triangle-free / no-C4 を導出する必要があった。`Set.toFinite`
  が `Fin 50` 上では自動で動くので証明はシンプル。
- `[DecidableRel Γ.Adj]` instance hyp は使わない場合 mathlib linter が
  warning。`induced_triangleFree` 等は decidability 不要なので削除。

### Lake build 状態

4041 jobs, clean。Stub 残数は不変 (新規補題はすべて proven)。CI ratchet OK。


## by claude (Tier B: spectral characters χ₀/χ₁/χ₂)

Tier B (Macaj-Siran §4 文字理論) 着手。`734e884`:

### 新規ファイル `Moore57Graph/Characters.lean`

- `chi0/chi1/chi2 : Equiv.Perm V → ℚ` を spectral projection trace
  `tr(E_λ · permMatrix σ)` で定義
- 基本恒等式: `chi0 = 1` (Moore57 specific), `χ₀+χ₁+χ₂ = a₀`,
  `a₁ = 57χ₀ + 7χ₁ − 8χ₂` (paper §2 Thm 1 逆向き)
- **共役不変性** `chi_j_conj : χⱼ(τστ⁻¹) = χⱼ(σ)` for graph aut `τ`:
  trace cyclic property + spectral projection commutation で素直に出る

### Theorem 1 inverse (§2)

`Section02_StateOfTheArt/Theorem1_Higman.lean` に paper の matrix `P`
の各行 (a₀/a₁/a₂ を χⱼ の ℤ-linear combination として) を追加。a₂ 行は
`χ₀ = 1` を経由して linear_combination で。

### §4 Lemma 11 char wrappers

`Lemma11_AiConstantOnClasses.lean` に
`lem11_chi{0,1,2}_constant_under_graphAut_conjugation` を追加。
これらは Theorem 3 (Curtis-Reiner) 不要 — 共役不変性は trace cyclic
property 1 つで足りる。rational class (coprime power) 不変性のみが
Theorem 3 を要する。

### 設計上の選択

- **Moore57 `permMatrix` は homomorphism**: Moore57 の `permMatrix σ`
  は `σ.symm.toPEquiv.toMatrix` なので、Mathlib の `Equiv.Perm.permMatrix`
  (antihomomorphism) と比べて真の準同型。`permMatrix_hom_mul` を local
  に再定義。
- **B2.1 bridge** は既存の `character_permutationRepresentation`
  (Mathlib の `Function.fixedPoints σ |>.ncard` 形) から Moore57 の
  `fixedVertexCount` (filter card) への変換。`Set.ncard_coe_finset`
  経由で 5 行で済んだ。
- **subrep の formal identification は deferred**: χⱼ を
  `Representation.character` of `range E_λ` subrepresentation として
  正式に identify するには、E_λ の range が G-stable subrep となる
  ことの証明が必要。今回は **関数として定義** + 共役不変性で済ませた。

### 残務

- B4.1 (rational class 不変性) → Theorem 3 external、Mathlib 未移植
- B3.1+ (full subrep identification) → 次のセッション候補
- B5.1 (Lem 14 paper-faithful 全形) → semi-regular 軌道分解で deferred-heavy

### Lake build 状態

4042 jobs, clean。新規 proven 補題 9 個。

## by claude (Tier B Lem 13/15/17/18/19 conditional rows)

Tier B "B5 部分撃破" 戦略で 2 commit:

### `b35143b` — fix-set monotonicity + Lem 13/15 conditional rows

- `Foundations/GroupAction/FixedPoints.lean`:
  - `fixedVertexSet_subset_pow`: `Fix(σ) ⊆ Fix(σⁿ)` (Mathlib `IsFixedPt.perm_pow`)
  - `fixedVertexCount_le_pow`: `a₀(σ) ≤ a₀(σⁿ)` (単純な subset card)
- `Lemma13_PrimeSquared.lean`:
  - `graphAut_pow`: graph aut の pow 伝播 (帰納法)
  - `lem13_p5_a0_zero_of_pow5_zero`: (p=5) row `(0, 0)` propagation
  - `lem13_p3_a0_le_one_of_pow3_one`: (p=3) row `(?, 1)` bound
  - `lem13_p3_row_1_1_pow3_a1_zero`: (p=3) row `(1, 1)` ⟹ Lem 12 p=3 starred
- `Lemma15_OrderPQ.lean`:
  - `lem15_no_pq_14_a0_49_conditional`: pq=14 row, `a₀(σ²) ∈ {2, 9}` + `a₀(σ) = 49`
    の矛盾 (`49 ≤ a₀(σ²) ≤ 9` omega)

### `51a70a5` — Lem 17/18/19 dispatch numeric bounds

- `Lemma17_3Group.lean`:
  - `lem17_orderOf_dvd_81_from_dispatch`: Petersen (∣27) ∨ singleton (∣81) ⟹ ∣81
  - `lem17_le_81_from_dispatch`: |X| ≤ 81
- `Lemma18_5Group.lean`:
  - `lem18_orderOf_dvd_125_from_dispatch`: HS (∣25) ∨ pentagon (∣5) ∨ empty (∣125) ⟹ ∣125
  - `lem18_le_125_from_dispatch`: |X| ≤ 125
- `Lemma19_LargePrime.lean`:
  - `lem19_le_19_from_dispatch`: prime per-case dispatch ⟹ |X| ≤ 19

### 設計上の選択

- **fix-set monotonicity の Foundations 化**: `IsFixedPt.perm_pow` を直接
  使った subset 引数。`fixedVertexCount_pow_coprime` (gcd=1 ⟹ 等号) と
  対照的に、一般 `n` で **≤ のみ** 成立する非自明な monotonicity。
- **`graphAut_pow`**: graph aut の hypothesis を `σ^n` に lift する一行
  helper。これがあれば Lem 13 → Lem 12 のような σ → σ^p reduction が
  簡潔に書ける。
- **dispatch theorems**: Theorem 4/5 が既に同パターンを採用しているので、
  Lem 17/18/19 にも揃えた。 paper-faithful な「Lem の結論を 1 行で
  書く」用途。

### Lake build 状態

4042 jobs, clean。Lem 13/15/17/18/19 で 8 個の新規 proven 補題。

## by claude (Tier B continuation: Lem 11 character chain + Lem 12/13 wrappers)

Tier B B5 拡張 3rd batch (3 commits):

### `f17f4df` — Lem 11 character chain + Lem 12 paper-faithful wrappers

- `Lemma11_AiConstantOnClasses.lean`:
  - `lem11_a0_via_characters`: `a₀(τστ⁻¹) = a₀(σ)` を `χ₀+χ₁+χ₂ = a₀`
    + chi conj invariance 経由で再導出 (paper-faithful character chain)
  - `lem11_a1_via_characters`: 同様に a₁ を `a₁ = 57χ₀+7χ₁−8χ₂` 経由
- `Lemma12_PrimeOrder.lean`:
  - `lem12_p2_a0_56` / `lem12_p2_a1_112` (involution wrap)
  - `lem12_p5_a0_dispatch` (`Nonempty (EmptyFixedData ∨ C5FixedData ∨ HSFixedData)`)
  - `lem12_p13_a0_zero_from_emptyFixedData`
  - `lem12_p3_a0_ten_from_petersenFixedData` (Lem 12 p=3 non-starred)
  - `lem12_a0_one_from_singletonFixedData`

### `9ab7eca` — Lem 13 FixedData-parameterised row bridges

- `lem13_p3_a0_le_10_via_petersenFixedData_pow3` (Petersen σ³ → a₀(σ) ≤ 10)
- `lem13_p5_a0_zero_via_emptyFixedData_pow5` (empty σ⁵ → a₀(σ) = 0)
- `lem13_p5_a0_le_50_via_HSFixedData_pow5` (HS σ⁵ → a₀(σ) ≤ 50)
- `lem13_p5_a0_le_5_via_C5FixedData_pow5` (pentagon σ⁵ → a₀(σ) ≤ 5)

### 観察

- **`Nonempty` wrap for FixedData dispatch**: FixedData は `Type` なので
  `Prop` 用 `Or` には直接入らない。`Nonempty (...)` でラップして
  `obtain ⟨h⟩ := ...` で展開する pattern。
- **Lem 12 p=3 starred の "mod-15 だけで撃破"**: 検証したところ、
  Theorem 1 形 `χ₁ = (8a₀+a₁−65)/15` は既に proven なので、χ₁ が
  integer なら mod-15 だけで `a₁ ≡ 12 (mod 15)` が出る。組み合わせて
  `a₁ = 0` (geometric) と矛盾。ただし χ₁(σ) ∈ ℤ for order-3 σ を
  示すには cyclotomic 機構が必要 (involution 限定の
  `exists_int_trace_of_involutive` を一般化する必要)。これは別タスク。

### Lake build 状態

4042 jobs, clean。3 commits, 17 個の新規 proven 補題。

## by claude (Tier B Section 3 unstub + Lem 11 a₂)

True-stub + 別名の proper signature がペアになっている stub を unstub
する整理を実行 (2 commits):

### `79cf986` — Cor 1 unstub + Lem 11 a₂ via characters

- `Corollary1_A1Le500.lean`:
  - `cor1_a1_le_500`: True-stub → `Moore57.adjacentMovedCount_le_500` の
    proper signature wrap
  - `cor1_adjacentMovedCount_le_500`: 別名として残す
- `Lemma11_AiConstantOnClasses.lean`:
  - `lem11_a2_via_characters`: a₂ := |V| - a₀ - a₁ の arithmetic 定義
    から、a₀ + a₁ conj-invariance の系として a₂ も conj-invariant

### `f167ac0` — Section 3 unstub

- `Lemma6_OrbitTrace.lean` `lem6_central_trace_le_two`: True-stub →
  `lem6_central_inducedTrace_le_two` の proper signature wrap
- `Lemma9_TraceFormula.lean` `lem9_orbit_trace_formula`: True-stub →
  `inducedTrace_orbit_count_formula_finite_group` wrap
- `Lemma10_TraceBounds.lean` `lem10_trace_bounds`: True-stub →
  `Moore57.mohar_trace_bounds` wrap

### 観察

- **「True-stub + proven 別名」アンチパターン**: 同じ paper-lemma が
  「placeholder 名」と「proper 名」の 2 つに分かれていた箇所が複数。
  歴史的に proper を後から追加した時に True-stub を残してしまった
  ものと思われる。unstub するだけで paper-name で直接呼べるようになる。
- **a₂ via characters**: a₂ は project では関数として独立に定義されて
  いない (= |V| - a₀ - a₁)。なので「a₂ の conj invariance」は a₀ と
  a₁ の系として linarith で自動。

### Lake build 状態

4042 jobs, clean。2 commits、5 個の旧 True-stub が proper-signature 化。
