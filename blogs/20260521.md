# 20260521

## by claude (Tier B B4.1: cyclotomic-based integer trace + Lem 12 p=3 unconditional)

戦略 ★ の **B4.1 cyclotomic-based integer trace for order p** を完成
させた (commit `4b9a6b9`)。

### `Foundations/LinearAlgebra/PowPrimeTrace.lean` (新規)

主結果: 任意の ℚ-線形自己準同型 `f : W →ₗ[ℚ] W` (有限次元) について
`f^p = 1` (p prime) なら `trace(f) ∈ ℤ`。

戦略:
- `cyclicProjection f p = (1/p) • (1 + f + f² + ... + f^(p-1))` を定義
- `s² = p·s` (cyclicSum_sq) より `cyclicProjection` は idempotent
- `range(proj) = ker(f - 1)` (固定部分空間), 上で `f` は恒等
- `ker(proj) = ker(cyclicSum f p) = ker(aeval f cyclotomic_p)` (p prime)
- DirectSum trace decomp 経由で `trace = dim(range) + trace(f|_{ker})`
- 上で既存 `trace_package_of_cyclotomic_prime_aeval_eq_zero` を適用:
  `dim(ker) = (p-1)·γ`, `trace(f|_{ker}) = -γ`
- 合計: `trace(f) = dim(range) - γ ∈ ℤ`

これは既存 `exists_int_trace_of_involutive` (p = 2) の **prime-order 一般化**。

### `Moore57Graph/Aut/TraceIntegrality.lean` 

新規 `aut_pow_prime_E7_trace_int`: σ が prime 位数 p の自己同型なら
`Matrix.trace (E7Matrix Γ * permMatrix σ) ∈ ℤ`。

Mathlib `Matrix.toLin'_pow` で `(permMatrix σ ^ p).toLin' = (permMatrix σ).toLin'^p`
を bridge にして、E7-range への restrict が prime-order を保つことを示し、
`exists_int_trace_of_pow_prime_eq_one` に流す。

### Lem 12 p=3 starred 完全 unconditional 化

`lem12_no_p3_a0_one` (True-stub) を実証明に置換:
1. `aut_pow_prime_E7_trace_int` (p=3): `tr(E₇·P_σ) ∈ ℤ`
2. `lem3_a1_mod_15`: `a₁ ≡ 7·a₀ + 5 (mod 15)`
3. `a₀ = 1`: `a₁ ≡ 12 (mod 15)`
4. `lem12_p3_a1_eq_zero` (no-triangle): `a₁ = 0`
5. `0 ≡ 12 (mod 15)` 矛盾

これは **論文の Theorem 3 (Curtis–Reiner) を完全に経由しない** 
unconditional 結果。Mathlib に Theorem 3 がない状況で、Moore57 特有の
mod-15 制約 (既に proven) を活用して同じ結論に到達。

### 観察

- **Cyclotomic 機構の最小単位**: 全 Galois 理論を建てる必要はなく、既存の
  `CyclotomicPrimeTrace.lean` (γ 個の Φ_p ブロックで trace = -γ) + 
  Bool-indexed `IsInternal` direct sum decomp で十分。
- **Polynomial restriction の壁**: `aeval (f.restrict h) q = 0` を示すために
  `Polynomial.aeval` の restrict 互換性を直接示すのは複雑。回避策として
  `cyclicSum (f.restrict h) p` を直接定義し、各 power について 
  `restrict_pow_apply` で underlying 一致を示す方式が clean。
- **Lake build 状態**: 自分の 3 ファイル (PowPrimeTrace, TraceIntegrality, 
  Lemma12_PrimeOrder) は clean ビルド。ただし他セッションで追加された 
  `Foundations/GraphTheory/AutSubgroup.lean` と 
  `Foundations/GroupTheory/SmallGroup625_12.lean` は別途エラーあり 
  (Moore57.lean に import 済み)、当作業の範囲外。

## by claude (Tier B B4.2: Lem 12 p=7 + Lem 13 p=3 starred unconditional)

ロードマップ §7.1 短期項目 7 = **B4.2 Starred p-prime row dispatch** を完了。
B4.1 で得た `aut_pow_prime_E7_trace_int` を横展開して以下の 2 つの
starred row を unstub:

### `lem12_no_p7_a0_58` (Lemma12_PrimeOrder.lean)

True-stub (`True := by trivial`) → 完全 unconditional な False 証明:

1. `haveI : Fact (Nat.Prime 7)` で B4.1 instance を構築
2. `aut_pow_prime_E7_trace_int hΓ σ hAut 7 hpow` → `tr(E₇·P_σ) ∈ ℤ`
3. `lem3_a1_mod_15` → `a₁ ≡ 7·58 + 5 = 411 ≡ 6 (mod 15)`
4. `lem12_a1_zero_of_closed_neighbourhood_fixed` → `a₁ = 0`
5. `0 ≡ 6 (mod 15)` 矛盾

**重要発見**: paper の character-theoretic lower bound `a₁ ∈ {21 + 105k}`
(Prop 2 依存, deferred-heavy) は **不要**。 mod-15 congruence だけで
p=3 と完全に同じ構造で contradiction が取れる。これにより
`lem12_no_p7_a0_58_conditional` (a₁≥21 仮定版) も実質的に上位互換に置換可能。

### `lem13_p3_row_1_1_no` (Lemma13_PrimeSquared.lean)

既存 `lem13_p3_row_1_1_pow3_a1_zero` は a₁(σ³)=0 を返す部分形だけだった。
これを「**Lem 13 p=3 starred row `(?, 1)` は不可**」という full False に強化。

戦略: σ^9 = 1 のとき σ³ は order-3 graph aut。 a₀(σ³) = 1 のとき
`lem12_no_p3_a0_one` (B4.1 経由) を σ³ に直接適用 → False。
`a₀(σ) ≤ a₀(σ³) = 1` 単調性で `(?, 1)` 列全体が排除される。

### B4.2 で確立した知見

- **Theorem 3 不要範囲が確定**: paper の starred row のうち
  **prime-order** のものはすべて B4.1 (= cyclotomic + 既存 mod-arithmetic)
  で Theorem 3 を bypass 可能。 残る Theorem 3 待ちは Lem 13 p=5 starred
  rows (`5*(0,5)`, `5*(5,5)`) など **composite-order** σ の Tr(σ) 整数性
  に限定。 これは B4.3 (Galois + generalized eigenspace) 案件で
  deferred-heavy。
- **コード量**: 各 row 30-50 LOC で完了、ロードマップ見積もり (50-100 LOC)
  より軽量。 B4.1 が十分強力な infrastructure を提供しているため。
- **CI ratchet 維持**: sorry/admit ゼロ、fully unconditional のまま
  True-stub が 2 件減少 (B4.1 で 1 件 + B4.2 で 1 件、累計 2)。

## by claude (Tier B B3.1+ partial: chi1 spectral subrepresentation)

ロードマップ §B3.1+ の χ₁ 部分を実装 (`Foundations/Representation/SpectralSubrepresentation.lean`)。
従来 `chi1 σ := Matrix.trace (E7Matrix Γ * permMatrix σ)` という直接定義
だけで、Mathlib `Representation.character` との formal な identification
は未だった。 これを完成。

### 構成

1. **`autSubgroupPermRep Γ : Representation ℚ (autSubgroup Γ) (V → ℚ)`** —
   graph aut subgroup の自然な permutation 表現。 既存
   `moore57_permMatrix_mul/one` で `map_one'/map_mul'` を構築。
2. **`chi1Subrep Γ : Representation ℚ (autSubgroup Γ) (range E₇.toLin')`** —
   既存 `Representation.onCommutingRange`
   (`D19OnMoore57/E7Projection/ProjectionRepresentationSkeleton.lean`) を
   `autSubgroupPermRep` + `E7Matrix.toLin' commute permMatrix` に適用。
3. **`chi1_eq_chi1Subrep_character`** — 主定理:
   `chi1 Γ g.val = (chi1Subrep Γ).character g` を、既存
   `trace_restrict_E7Range_permMatrix_toLin'_eq_matrix_trace`
   (`ProjectionTraceBridge.lean`) で 3 行に書ける。

### 観察

- **既存 infrastructure の再利用度が高い**: D19 specific だった
  `Representation.onCommutingRange` (commuting endo の range への restrict
  helper) と E7 trace bridge を generic な `autSubgroup` 版で呼ぶだけで、
  新規証明はほぼゼロ。`Representation.character` の trace 定義が
  `LinearMap.trace`-based なので、既存 LinearMap-level の bridge が直結。
- **χ₀, χ₂ 拡張は別 commit**: `E_57` (全 perm と可換、軽い) と
  `EMinus8Matrix` (既存 `minus8Matrix` 形だが命名 alias bridge 要) は
  同パターンで素直だが、各々 ~30-50 LOC の boilerplate が必要なので
  分離。
- **意義**: χ₁ が一次的に「特定の subrep の trace」として扱える事を
  Lean レベルで明示。 将来的 character 系の議論 (Schur 直交性, 既約性
  判定など) に向けた足場としての位置づけ。
