# 20260517

Order22 Phase D: trace identity の spectral side を完全証明 + 戦略再評価.

## Phase D 進展

### 進展サマリー

前日 (`20260516.md`) 時点で残った 2 sorries:
- `aF11_lambda_seven_eq_159`: dim(V_7 ∩ ker T_F11) = 159.
- `trace_identity_via_F11_spectral`: 10n ≡ Σ λ a_F11_λ mod 11.

今日の作業: `trace_identity_via_F11_spectral` を 2 つに **構造的分解**.

### Trace identity spectral side 完全証明 (commit `5016452`)

`IsIdempotentElem.isProj_range` + `IsProj.trace` 経路 (~285 行新規):

```lean
private theorem trace_E_λ_restrict_ker_T_eq :
    LinearMap.trace (ZMod 11) _ h.E_λ_restrict_ker_T = (a_F11_λ : ZMod 11)
```

を 3 つの λ で証明 (idempotency lemma + range identification + finrank_map_subtype).

`adjMatrixF11_restrict_eq_smul_sum`:
```lean
A_restrict = 2 • E_2_restrict + 7 • E_7_restrict + 3 • E_3_restrict
```
を ELambda_decomp_A (matrix 等式) から lift.

合成:
```lean
theorem trace_adjMatrixF11_restrict_eq_spectral_side :  -- sorry-free
    LinearMap.trace _ _ A_restrict = 2 * a_F11_2 + 7 * a_F11_7 + 3 * a_F11_3
```

`trace_identity_via_F11_spectral` 本体は orbital + spectral の合成で **sorry-free**.

### 残 sorry の narrowing

| 旧 | 新 |
|---|---|
| `trace_identity_via_F11_spectral` (sorry, 1 big) | `trace_adjMatrixF11_restrict_eq_orbital_side` (sorry, narrower) |
| `aF11_lambda_seven_eq_159` (sorry) | 不変 |

Sorry 数: 2 → 2 (count 不変, narrowing 達成).

## 戦略再評価 (commit pending)

User 指示で web search + papers_info.md 精査:

### 主要発見

1. **Makhnev (arxiv 2010.13443, 2020)**: 三重交差数 + 配列対称化アプローチ. **私たちの F_11 spectral と独立**.

2. **Macaj-Siran 2010** (`tmp/pdfs/j.laa.2009.07.018.txt`): 自然言語証明の本筋. Cameron's Higman trace formula を ℚ 上で character theory と組合せる.
   - **Lemma 12**: σ order 11, a_0(σ)=5 ⟹ a_1(σ) ∈ {55, 220, 385} ⟹ n ∈ {5, 20, 35}.
   - **Lemma 14-15**: 直積分解 + 半正則作用で additional constraints.

3. **既存 Lean infrastructure の整合**:
   - `Tk_eq_eleven_mul_traceNumber : T_k = 11n` (sorry-free) は Macaj-Siran Theorem 1 と整合.
   - `funLeft (Quotient.mk s)` で ker T_F11 ≃ (V/σ → F_11) も既存.

### Critical insight

**Trace identity + Phase 3 だけでは (n, a_7) pair は pin down できない**:

検証 (4 candidates from Phase 3 + 3a = 2n + 467):
| n | a_7 | 4a_7 + 8 mod 11 | 10n mod 11 | 整合 |
|---|---|---|---|---|
| 5 | 159 | 6 | 6 | ✓ |
| 20 | 169 | 2 | 2 | ✓ |
| 35 | 179 | 9 | 9 | ✓ |
| 50 | 189 | 5 | 5 | ✓ |

4 候補すべて trace identity を満たす. 従って `a_F11_7 = 159` (good reduction) が
**両ルートの本質的 bottleneck**.

### 戦略選択

User 選択: orbital trace identity の実装を優先 (aF11_lambda_seven は別 session).

理由:
- orbital side は既存 infrastructure (Tk_constant + Quotient iso) で実装可行.
- aF11_lambda_seven_eq_159 は Mathlib に未整備の DVR 好還元理論を要する.

### Memory 更新

- `reference_moore57_papers.md` 新規作成: Macaj-Siran, Makhnev, Cameron 等の参照と位置付け.
- `MEMORY.md` index 更新.
- `project_moore57_state.md` 更新: 戦略再評価の知見を記録.

### orbital trace identity の数学的 outline (docstring に追加済み)

```
Step 1: trace(A · P_int) = 110n over ℤ (Σ T_k for k=0..10)
Step 2: P_int(u,v) = 11 if u=v=fixed, 1 if u,v same free orbit, else 0
Step 3: trace(A · P_int) = Σ_v free |N(v) ∩ orbit(v)| over ℤ
Step 4: σ-invariance: 11 · |N(v_O) ∩ O| = Σ_v∈O |N(v) ∩ O|
Step 5: 軌道基底 trace = Σ_O |N(v_O) ∩ O|
Step 6: = (1/11) · 110n = 10n over ℤ. Mod 11: 10n mod 11.
```

## 次セッションへの引継ぎ

1. orbital trace identity の実装 (~200-300 行):
   - `kerTF11_quotientEquiv : (Quotient s → F_11) ≃ₗ ker T_F11` の top-level 抽出.
   - `LinearMap.trace_conj` で trace を Quotient 側に転送.
   - `LinearMap.trace_eq_matrix_trace` + `Pi.basisFun` で対角和に展開.
   - 各対角 = `|N(v_O) ∩ O|` を直接計算.
   - 総和 = `10n mod 11` を Tk_eq_eleven_mul_traceNumber で確立.

2. aF11_lambda_seven_eq_159: DVR 好還元理論の整備が必要. 
   - Alternative: rank inequality (rank ≤ baseChange) を直接 ad-hoc に書く.
   - dim V_7 ≥ 1729 over F_11 + dim V_3 ≥ 1520 over F_11 + Σ = 3250 ⟹ 等式.
   - 約 ~300-500 行.

## 追加進展 (同日, 8 commits)

軌道側 trace identity の Phase 1-5 を実装 (Phase 6 のみ残):

| Commit | Sorry-free 追加 |
|---|---|
| `a794e03` | `kerTF11_quotientEquiv`, `adjMatrixF11_quot`, `trace_adjMatrixF11_restrict_eq_trace_quot` |
| `41a1876` | `trace_adjMatrixF11_quot_eq_sum` |
| `6aa2f11` | `kerTF11_quotientEquiv_basisFun_apply` |
| `27058d3` | `adjMatrixF11_restrict_ker_T_basisFun_val` (`rfl` proof!) |
| `71cb7fd` | `orbitNeighborSumF11`, `orbitNeighborSumQuot`, `kerTF11_quotientEquiv_orbitNeighborSumQuot_eq`, `adjMatrixF11_quot_basisFun_apply_at_mk` |
| `2ca7d78` | `orbitNeighborSumF11_sigma_invariant` (σ-不変性 sorry-free!) |

これで orbital side sorry は以下に **narrow** された:

```lean
trace(A_restrict_F11)
  = trace(A_quot)                                       -- ✓ 完成 (trace_eq_via_conj)
  = Σ_O (A_quot (Pi.basisFun _ _ O)) O                  -- ✓ 完成 (trace_eq_sum)
  = Σ_O (= sum involving Quotient.out O)                -- ✓ 完成 (apply_at_mk)
  = (10n : F_11)                                        -- ←残 (Tk-based integer identity)
```

つまり, あと **「Σ_O |N(rep) ∩ O| = 10n の整数恒等式 + F_11 cast」** だけで closure.

### 重要な発見

* `adjMatrixF11_restrict_ker_T_basisFun_val` は **`rfl` で閉じる**:
  A_restrict の forward 方向 (e 経由) は完全に definitional.
* `kerTF11_quotientEquiv.symm` は非自明 (Quotient.lift 構造あり).
  従って A_quot 対角は明示的 candidate function + `LinearEquiv.symm_apply_apply` 経由.

## Order22 全体状態 (本日終了時点)

| Phase | 状態 |
|---|---|
| 1 (rotation a_0) | 完成 |
| 2 (Tk_constant, eleven_dvd_Tk) | 完成 |
| 3 (n ∈ {5,20,35,50}) | 完成 |
| 4 spectral side | 完成 (本日) |
| 4 orbital side basis machinery | 完成 (本日) |
| 4 A_quot diagonal = neighbor sum (σ-不変経由) | 完成 (本日) |
| 4 σ-invariance of orbitNeighborSumF11 | 完成 (本日) |
| 4 trace = vertex-sum (Σ_w (adj) (rep_w) w) | **完成 (本日 commit cce6f46)** |
| 4 integer identity count = 10n | **残** (next session, ~60-100 行) |
| 4 a_F11_7 = 159 | **残** (deep, good reduction) |
| 5 traceNumber even (cyclic + dihedral) | 完成 |

主結果 `no_Order22_acts_on_Moore57` は 2 sorries に依存. 軌道側 trace identity の
**約 90%** が今日 sorry-free 化された. 残るは最終的な整数恒等式 (count = 10n via
Tk_eq_eleven_mul_traceNumber + orbit-bijection by slope k) のみ.

## 本日の最終状態 (commit `a4986ea`)

軌道側 trace identity への chain (全 sorry-free):

```
trace(A_restrict | ker T_F11)
  = trace(A_quot)                                       -- trace_adjMatrixF11_restrict_eq_trace_quot
  = Σ_O (A_quot (Pi.basisFun O)) O                      -- trace_adjMatrixF11_quot_eq_sum
  = Σ_O orbitNeighborSumF11 O (Quotient.out O)          -- diagonal_eq_orbitNeighborSumF11_out
  = Σ_w (adjMatrixF11 Γ) (Quotient.out (Quotient.mk w)) w  -- sum_over_vertices
  = ((count : ℕ) : F_11)                                -- Finset.sum_boole (next step)
  = (10n : F_11)                                        -- count = 10n via Tk (final step)
```

最後の 2 ステップ (`count = 10n` integer identity) は 60-100 行と推定. 
`Tk_eq_eleven_mul_traceNumber` + σ-orbit bijection で完結する見込み.

## Order22 全体状態

| Phase | 状態 |
|---|---|
| 1 (rotation a_0) | 完成 |
| 2 (Tk_constant, eleven_dvd_Tk) | 完成 |
| 3 (n ∈ {5,20,35,50}) | 完成 |
| 4 spectral side | 完成 (本日) |
| 4 orbital side | sorry (narrower) |
| 4 a_F11_7 = 159 | sorry (deep) |
| 5 traceNumber even (cyclic + dihedral) | 完成 |

主結果 `no_Order22_acts_on_Moore57` は 2 sorries に依存.

## C3 bijection 完了 (commit `36d2f09`)

orbital side trace identity の最終ステップ **C3 bijection** を実装し,
`trace_adjMatrixF11_restrict_eq_orbital_side` および
`trace_identity_via_F11_spectral` を sorry-free 化.

### 新規 helper lemmas (~380 行)

1. **`orbitNeighborCountN : V → ℕ`** + σ-step + SameCycle invariance.
2. **`σ_pow_apply_eq_self_iff_dvd_of_free`**: 自由軌道で `σ^n rep = rep ⟺ 11 ∣ n`.
   Mathlib `IsCycle.pow_eq_one_iff'` + `cycleOf_pow_apply_self` + `cycleType_σ_eq_replicate` 経由.
3. **`σ_pow_apply_injective_of_free`**: 素数性 11 から `σ^d : Fin 11 → V` の単射性.
4. **`σ_fiber_eq_singleton_of_fixed`**, **`σ_fiber_card_eq_eleven_of_free`**:
   軌道濃度. 自由軌道は σ^d 直接 bijection で構築 (cycleOf 経由を回避).
5. **`cycleOf_support_card_eq_eleven`**: cycleType replicate から導出.

### C3 本体構造

```
Σ_v onc v
  = Σ_O Σ_(v ∈ fiber O) onc v             -- sum_fiberwise_of_maps_to
  = Σ_O 11 * onc(rep_O)                   -- σ-inv + case split (free: 11倍, fixed: 0)
  = 11 * Σ_O onc(rep_O)                   -- mul_sum

count = Σ_O |{w ∈ fiber O : Adj rep_O w}|   -- sum_fiberwise_of_maps_to + sum_boole
      = Σ_O onc(rep_O)                     -- σ^d bijection for free, 0=0 for fixed

11 * count = 11 * Σ_O onc(rep_O) = Σ_v onc v = 110n
⟹ count = 10n (Nat.eq_of_mul_eq_mul_left).
⟹ (count : F_11) = (10n : F_11).
```

### 残 sorry

| Phase | 状態 |
|---|---|
| 4 spectral side | 完成 |
| 4 orbital side | **完成 (本日 C3)** |
| 4 a_F11_7 = 159 | sorry (deep, good reduction) |

主結果 `no_Order22_acts_on_Moore57` は **1 sorry** に依存
(`aF11_lambda_seven_eq_159`, DVR 好還元理論).

### 技術的ハマりどころ

* `[DecidableRel (SameCycle.setoid h.σ).r]` vs Mathlib auto `h.σ.instDecidableRelSameCycle`:
  cycleOf 経由で型不一致発生. 直接 `σ^d : Fin 11 → V` bijection で回避.
* `mem_support_cycleOf_iff` の rewrite が pattern matching 失敗.
  → `Finset.card_bij` で直接構築する側に移行.
* `Int.emod_add_ediv` is deprecated (use `Int.emod_add_mul_ediv`) — 既存利用箇所と新コードで `linarith` 経由対応.

### Generalization

`sigma_invariant_zpow` を `{α : Type*}` に一般化 (was `V → ZMod 11`).
これで `orbitNeighborCountN : V → ℕ` でも利用可能に. ([[feedback_generalize_theorems]])

## 戦略分析: 残 sorry `aF11_lambda_seven_eq_159` (commit `26be536`)

`Phase4F11Spectral.lean` の docstring に hybrid B+A 戦略を詳細展開.

### 数学的構造

(a) **Jordan 単調性 (Option B)**: `g_λ(j) = dim(V_λ ∩ ker T_F11^j)` concave.
    `Σ g_λ = 5 + 295 j` (linear) ⟹ 各 g_λ も linear on [1, 11].
    ⟹ V_λ = M_1^{l_λ} ⊕ M_11^{k_λ} (size 1 と 11 のブロックのみ).

(b) **σ-trace identification**: `trace(σ · E_λ) over F_11 = l_λ`.
    各 M_11 で trace σ = 11 ≡ 0 (mod 11), M_1 で trace σ = 1.
    Σ l_λ = 5 (= #Fix σ). Computed values: l_2=1, l_7=2, l_3=2.

(c) **dim V_λ over F_11 (Option A; good reduction)**: 主要 bottleneck.
    `dim V_7 over F_11 = 1729` を ℚ-rank からの reduction で.
    Mathlib に直接 lemma 不在, 自前構築要 (charpoly / Smith form 経由).

### 残作業 (~600-1000 行, 多session work)

1. Concave linearity for V_λ (B framework): ~200-300 行.
2. σ-trace identification (KS-based): ~150-250 行.
3. dim V_λ via good reduction: ~250-450 行.
4. Final wire-up: ~50 行.

### 次セッション entry point

- 最も tractable な開始点: σ-trace 計算 (sorry-free, ~100 行).
  - trace(P_σ · E_λ) over F_11 = (1, 2, 2). 既存の matrix arithmetic 利用.
- 続いて concave linearity for V_λ (Option B).
- 最後に good reduction for dim V_λ (Option A).

## Order22 全体状態 (2026-05-17 終了時点)

| Phase | 状態 |
|---|---|
| 1 (rotation a_0) | 完成 |
| 2 (Tk_constant) | 完成 |
| 3 (n ∈ {5,20,35,50}) | 完成 |
| 4 spectral side | 完成 |
| 4 orbital side | **完成 (本日 C3)** |
| 4 a_F11_7 = 159 | sorry (詳細戦略 documented) |
| 5 traceNumber even | 完成 |

主結果 `no_Order22_acts_on_Moore57` は **1 sorry** に依存 (`aF11_lambda_seven_eq_159`).

## Phase D-C 完成 + Phase D-B 大幅進展 (本日後半, commits b2beab6..d248111)

戦略分析を受けて段階実装で進めた:

### Phase D-C (sorry-free, ~130 行)

`trace(E_λ · P_σ)` over F_11 を closed forms 経由で計算:
- `trace_permMatrixF11_eq_fixedVertexCount` (general).
- `trace_adjMatrixF11_mul_permMatrixF11_eq_adjacentMovedCount` (general).
- `trace_allOnesMatrixF11_mul_permMatrixF11` (general).
- Order22 specializations: (5, 0, 5) (= #Fix σ, T_1 mod 11, |V| mod 11).
- `trace(E_2 · P_σ) = 1`, `trace(E_7 · P_σ) = 2`, `trace(E_3 · P_σ) = 2`.

Σ = 1 + 2 + 2 = 5 = #Fix σ ✓.

### Phase D-B 進展 (sorry-free, ~470 行)

1. **V_λ-invariance of T_F11**: V_2, V_7, V_3 はそれぞれ T_F11 で保たれる.
2. **`T_F11_V_λ`** restricted LinearMap (V_λ →ₗ V_λ).
3. **Subtype correspondence**: `(T_F11_V_λ)^j x.val = (T_F11)^j x.val`.
4. **`map_subtype_ker_T_F11_V_λ_pow_eq`**: subtype 経由で ker (T_F11_V_λ)^j → V_λ ⊓ ker T_F11^j.
5. **`finrank_ker_T_F11_V_λ_pow_eq`**: finrank 同一性.
6. **E_λ preserves ker T_F11^j (各 j)**: `_commute_preserves_ker_T_F11_pow` (general).
7. **`ker_T_F11_pow_eq_sup_three`**: 各 j で ker T_F11^j = ⊔_λ (V_λ ⊓ ker T_F11^j).
8. **Pairwise disjoint**: V_λ ⊓ ker T_F11^j の pairwise disjoint + 3-way.
9. **`finrank_sum_V_lambda_inter_ker_T_F11_pow`**: Σ_λ = finrank ker T_F11^j.
10. **Concavity**: `finrank_V_λ_inter_ker_T_F11_pow_concave` via `finrank_ker_pow_concave` on T_F11_V_λ.

### Phase D-B 残作業 (~100 行)

「凹+和線形 ⟹ 各線形」forcing:
- Σ concavity ≤ + Σ g_λ = g_total linear ⟹ 各 concavity = equality.
- ⟹ 各 g_λ linear on [1, 11] with slope k_λ.

### Phase D-A 未着手

dim V_λ over F_11 = (1, 1729, 1520) を good reduction で確立.
- Mathlib `Matrix.charpoly_map` + diagonalizability arguments.
- Or: rank inequality under reduction.

### Phase D-D 未着手

l_λ + k_λ + dim V_λ で a_F11_λ = (1, 159, 140) を導出して
`aF11_lambda_seven_eq_159` を閉じる.

## Total progress (本日)

C3 bijection (前半) + Phase D-C 完成 + Phase D-B 大幅進展:
合計 ~1000+ 行の新規 sorry-free 補題追加.

主結果 `no_Order22_acts_on_Moore57` は依然 1 sorry 依存, ただし
sorry までの構造的 path は大幅に整理された.

## Phase D-B finalize 完成 (commit `d07fc26`)

「凹+和線形 ⟹ 各線形」forcing application を実装 (~347 行 sorry-free):

### 主要結果

**`dim_V_λ_plus_9_aF11_λ_eq_10_g_λ_2`** (各 λ ∈ {2, 7, 3}):
`dim V_λ + 9 * a_F11_λ = 10 * g_λ(2)`
すなわち linearity formula at j = 11.

### 証明手順

1. **`g_V_lambda_linearity_inner_eq`** (concavity ⟹ equality):
   Σ_λ concavity ≤ + Σ g_λ linear (= 5+295j on [1,11])
   ⟹ 各 concavity ≤ = equality (j ∈ [2, 10]).

2. **`g_V*_difference_eq_at_inner`**: 差分形 g(j+1) - g(j) = g(j) - g(j-1) for j ∈ [2, 10].

3. **`g_V*_linear_add_form`** (additive linear, 帰納で構築):
   `g_λ(j+1) + g_λ(1) = g_λ(j) + g_λ(2)` for j ∈ [1, 10].

4. **`ker_T_F11_pow_eleven_eq_top`**: T_F11^11 = 0 ⟹ ker = ⊤.

5. **`g_V*_eleven_eq_dim`**: g_λ(11) = dim V_λ (via inf_top_eq).

6. **`dim_V*_plus_9_aF11_*_eq_10_g2`** (Phase D-B main): ℤ casting + linarith telescoping.

### 技術的ハマりどころ

* `(k+1) - 1` の ℕ 引き算問題: omega が `Nat.sub` を symbolic 扱い.
  → `rw [h_idx]` で explicit に `k+1-1 = k` 適用.
* ℕ 引き算で linarith が失敗: ℤ にキャストして処理.
* `simp only [hg_def]` で `g j` を展開してから `exact_mod_cast`.

## Phase D-B 完成! Phase D-A / D-D 残

Phase D-B 全体は完成 (sorry-free).

### 残作業

- **Phase D-A** (~250-450 行): dim V_λ over F_11 = (1, 1729, 1520).
  良還元 (charpoly via Matrix.charpoly_map) or rank inequality 経由.
- **Phase D-D** (~100 行): wire-up.
  - dim V_λ + Σ l_λ = 5 + integer constraint で a_F11_λ = (1, 159, 140).
  - aF11_lambda_seven_eq_159 を close.

### Σ dim V_λ = 3250 over F_11

Phase D-A の前準備として `V2_sup_V7_sup_V3_eq_top` (sorry-free) +
pairwise disjoint で Σ_λ dim V_λ = 3250 を導出可能.

これがあれば dim V_7 = 1729 + 既存 dim V_2 = 1 から dim V_3 = 1520 を導出
sorry-free に.

主結果 `no_Order22_acts_on_Moore57` は依然 1 sorry 依存.
合計新規コード: **~1300+ 行 sorry-free** (本日, C3 含む).

## Phase D-A スケルトン + Phase D-D wire-up 完成

### Phase D-A スケルトン (~80 行 sorry-free + 1 focused sorry)

`V2_sup_V7_inter_V3_eq_bot` (sorry-free, ~40 行) + 既存
`V2_sup_V7_sup_V3_eq_top` + pairwise disjoint で:

```lean
theorem finrank_sum_V_lambda_eq_3250 :
    finrank V_2 + finrank V_7 + finrank V_3 = finrank (V → F_11)  -- sorry-free
theorem finrank_sum_V_lambda_eq_card_V :
    finrank V_2 + finrank V_7 + finrank V_3 = 3250  -- sorry-free
```

これで dim V_7 = 1729 を仮定すれば dim V_3 = 1520 が自動導出:

```lean
theorem finrank_V7Submodule_eq_1729 :
    finrank V_7 = 1729 := sorry  -- focused Phase D-A sorry (good reduction)

theorem finrank_V3Submodule_eq_1520 :
    finrank V_3 = 1520 := by  -- sorry-free
  have h_sum := finrank_sum_V_lambda_eq_card_V h
  have h_2 := finrank_V2Submodule_eq_one h
  have h_7 := finrank_V7Submodule_eq_1729 h
  omega
```

### Phase D-D wire-up: `aF11_lambda_seven_eq_159` 閉鎖 (~60 行)

Phase D-B (linearity) + Phase D-A (dim V_λ) + concavity を組み合わせ:

```lean
theorem aF11_lambda_seven_eq_159 :
    finrank (V_7 ⊓ ker T_F11) = 159 := by
  -- (1) dim V_λ + 9 a_F11_λ = 10 g_λ(2)   (Phase D-B 線形性)
  -- (2) g_λ(2) ≤ 2 a_F11_λ                (concavity at j=1, g_λ(0)=0)
  -- (3) a_F11_2 + a_F11_7 + a_F11_3 = 300 (direct sum)
  -- (4) a_F11_2 = 1                       (V_2 = span(1_V))
  ...
  omega
```

数学的推論:

* h_lin_7: `1729 + 9 a7 = 10 b7`. Mod 10 で `9 a7 ≡ 1 (mod 10)` ⟹ `a7 ≡ 9 (mod 10)`.
* h_concave_7 + h_lin_7: `1729 + 9 a7 ≤ 20 a7` ⟹ `a7 ≥ 158`.
* h_concave_3 + h_lin_3: `a3 ≥ 139` (1520/11 = 138.18 切上).
* h_sum + h_a2: `a7 + a3 = 299`, `a3 ≥ 139` ⟹ `a7 ≤ 160`.
* 結論: `a7 ∈ [158, 160] ∩ {a : a ≡ 9 (mod 10)} = {159}`. ⟹ `a7 = 159`. ✓

これらは全て **omega** が ℕ 上で自動処理.

**重要**: σ-trace identification (Phase D-C) は不要だった.
dim V_λ + Σ a_F11_λ = 300 + concavity だけで一意決定可能.

### 残 sorry: 1 個 (focused on Phase D-A)

```
Moore57/Order22OnMoore57/Phase4F11Spectral.lean:2250:  sorry
```

`finrank_V7Submodule_eq_1729` のみ. すなわち:

> Moore57 隣接行列 A の固有値 7 (重複度 1729 over ℚ) は mod 11 reduction で
> 重複度を保つ (good reduction).

これさえ閉じれば `no_Order22_acts_on_Moore57` 全体 sorry-free.

### 進捗統計

- Phase D-B 線形性 (`g_V*_linear_add_form`): sorry-free
- Phase D-B 線形 ⟹ 主関係式 (`dim_V*_plus_9_aF11_*_eq_10_g2`): sorry-free
- Phase D-A `Σ dim V_λ = 3250`: sorry-free
- Phase D-D wire-up (`aF11_lambda_seven_eq_159`): sorry-free
- Phase D-A `dim V_7 = 1729`: **focused sorry** (Phase E に向けて)

総 sorry 数: **1** (D19, Order22 主全体, Phase4 含む).

## 🎉 Phase D-A 完成 - Order22 完全 sorry-free!

同セッション内で Phase D-A の 7 steps を順次形式化し最終 sorry を閉じた:

### Step 1: V_7 = ker(A - 7•I) (sorry-free, ~60 行)

E_7 closed form `E_7 = 2 + 3 A + 5 J` + `J·A = 2 J` から:
v ∈ ker(A - 7) ⟹ J v = 0 (via `5 J v = 0`).
E_7 v = 2v + 21v + 0 = 23v = v.

### Step 2: A_F11.toLin' IsSemisimple (sorry-free, ~65 行)

`(X-2)(X-7)(X-3)` が F_11 上 Squarefree ⟹ `isSemisimple_of_squarefree_aeval_eq_zero`.

### Step 3: V_7 = maxGenEigenspace 7 (sorry-free, ~15 行)

Semisimple + `IsFinitelySemisimple.maxGenEigenspace_eq_eigenspace`.

### Step 4: dim V_7 = charpoly.rootMultiplicity 7 (sorry-free, ~10 行)

`LinearMap.finrank_maxGenEigenspace_eq`.

### Step 5: adjMatrixF11.charpoly = (Γ.adjMatrix ℤ).charpoly.map (sorry-free, ~40 行)

`Matrix.charpoly_map` + Int.castRingHom リダクション.

### Step 6: ℚ-side rootMultiplicity 下限 (sorry-free, ~150 行)

各 λ ∈ {57, 7, -8}:
- A_ℚ · E_λ = λ • E_λ (spectral decomposition から).
- range E_λ ⊆ Module.End.eigenspace A_ℚ.toLin' λ.
- `LinearMap.finrank_eigenspace_le` で rootMultiplicity λ ≥ dim V_λ_ℚ.
- 結果: rootMult 57 ≥ 1, rootMult 7 ≥ 1729, rootMult -8 ≥ 1520.

### Step 7a: ℤ → F_11 bridge (sorry-free, ~85 行)

- ℤ → ℚ injection (`eq_rootMultiplicity_map`): ℤ-rootMult = ℚ-rootMult.
- ℤ → F_11 (`le_rootMultiplicity_map`): F_11-rootMult ≥ ℤ-rootMult.
- 結果: F_11 で rootMult 2 ≥ 1, rootMult 7 ≥ 1729, rootMult 3 ≥ 1520.

### Step 7b: Sum forcing + 主 wire-up (sorry-free, ~100 行)

- `sum_three_rootMultiplicity_le_natDegree`: 3 distinct roots の rootMult sum
  ≤ natDegree. pairwise IsCoprime (X - C a)^m × ... | p の degree 議論.
- adjMatrixF11.charpoly.natDegree = 3250.
- 下限 1 + 1729 + 1520 = 3250 = 上限 ⟹ 等号成立.
- rootMult 7 = 1729 exactly.
- `finrank_V7Submodule_eq_1729 = rootMult 7 = 1729` で wire-up.

### 主結果

```lean
theorem no_Order22_acts_on_Moore57 :
    ¬ Nonempty (Order22ActsOnMoore57 V Γ)
```

D19 と Order22 両方とも完全 sorry-free.
Moore57 graph 上の位数 22 自己同型群非存在を **完全形式化** 完了.

### 工程概観

| Phase | 内容 | sorries |
|-------|------|---------|
| Phase 1-3 | 既存 (D19, σ-trace constancy, four-cycle bound) | 0 |
| Phase 4 | F_11 spectral framework + trace identity (本日中盤完成) | 0 |
| Phase D-B | ker T^j concavity + linearity 強制 | 0 |
| Phase D-A | 良還元 (charpoly_map + eigenspace bridge) | 0 |
| Phase D-D | aF11_lambda_seven_eq_159 wire-up | 0 |
| **Total** | | **0** |

## 追補: Order22 conditional 入力削減 (夜)

完全 sorry-free を達成した上で, conditional 入力 3 つを 1 つずつ削減する段階に
入った. まず **τ_fix : K155FixedData Γ τ** を構造体フィールドから derived def に
格上げ:

### 変更箇所

* [Moore57/Order22OnMoore57/Action.lean](../Moore57/Order22OnMoore57/Action.lean):
  - `τ_fix : K155FixedData Γ τ` フィールドを構造体から削除.
  - `Moore57.Moore57Graph.Aut.InvolutionFixIsK155` を import.
  - `noncomputable def Order22ActsOnMoore57.τ_fix : K155FixedData Γ h.τ` を追加:
    `(aut_involution_nonempty_K155FixedData h.isMoore h.τ h.τ_aut h.τ_involutive h.τ_ne_one).some`.

### 効果

* conditional 入力が 3 → 2 個に減少.
* `h.τ_fix.center`, `h.τ_fix.leaf` 等の既存呼び出しは dot notation で透過的に
  動作 (Parity.lean は無修正でビルド成功).
* 主結果 `no_Order22_acts_on_Moore57` の axiom set は変化なし
  (`propext`, `Classical.choice`, `Quot.sound` の 3 軸のみ).

### 残 conditional 入力

* `σ_fix : C5FixedData Γ σ` — Fix(σ) = C_5. 自然言語証明 §1 の基本構造.
  σ の固定点 5 個が cycle を成すことを示す必要があり, 重い (Macaj-Siran Lemma 12).
* `fix_τ_adj_σ_card_even` — dihedral case の F_x fiber 構造から導かれる偶数性.

### D19 側 simplification 第 1 弾 (Tier-2 routing)

D19 の `fixedVertexCount_reflection_eq_56_of_raw_action` を Tier-2
`Moore57.aut_involution_fixedVertexCount_eq_56` (Cameron Theorem 3.13, sorry-free)
経由で書き換え:

* [Moore57/D19OnMoore57/Reflection/InvolutionFixedSetStarFromActionBoundary.lean](../Moore57/D19OnMoore57/Reflection/InvolutionFixedSetStarFromActionBoundary.lean):
  - `reflection_smulEquiv_ne_one` (新規) を追加: faithfulness + constructor distinctness
    で reflection が非自明置換であることを示す.

* [Moore57/D19OnMoore57/Reflection/AdjacentMovedPositive.lean](../Moore57/D19OnMoore57/Reflection/AdjacentMovedPositive.lean):
  - 旧版: `fixedVertexCount_reflection_trace_refined_le_fiftySix`
    (7 候補 {6,10,16,26,36,46,56} を絞る) +
    `reflection_adjacentMovedCount_pos` (a₁ > 0 from edge formula) →
    `fixedVertexCount_reflection_eq_56_of_adjacentMovedCount_pos`
    (Cameron Step 2 複製) で `_eq_56_of_raw_action` を導出.
  - 新版: Tier-2 `aut_involution_fixedVertexCount_eq_56` に直接 reduce.
    `reflection_adjacentMovedCount_pos` は `|Fix| = 56` から逆に導出する形に整理.

* [Moore57/D19OnMoore57/E7Projection/Minus8RotationSplit.lean](../Moore57/D19OnMoore57/E7Projection/Minus8RotationSplit.lean):
  - 旧 transitive import を explicit に: `import AdjacentSwapFixedCount` 追加.

### 効果

* `AdjacentMovedPositive.lean`: 70 行強の論証が **数行の Tier-2 呼び出し** に圧縮.
* 主結果 (D19, Order22, C38) すべて axiom set 不変.
* Build 3856 jobs 成功.

### 注記

`AdjacentSwapFixedCount.lean` 自身は `fixedVertexCount_reflection_ge_fiftySix_of_adjacent_swap`
等の別 export が `Minus8RotationSplit.lean` で残り使用されているため削除せず.
`TraceSmallCandidateReduction.lean` も他経路で必要なため残置.
本セッションは「依存グラフの一部を Tier-2 経由に短絡する」軽量 simplification.

## Conditional 入力削減 Phase 2: `fix_τ_adj_σ_card_even` 解消 (進行中)

`Order22OnMoore57/Dihedral/` サブディレクトリに 6 ファイル scaffold,
段階的に sorry を解消 (ロードマップ
[notes_fix_tau_adj_sigma_card_even_roadmap.md](../notes_fix_tau_adj_sigma_card_even_roadmap.md)).

### 全体方針

自然言語証明 §5.2 を **parity に簡略化**:

```
ρ_1 := τ ∘ π_1 : F_0 → F_0 は involution
|F_0| = 56 (even)
|Fix(ρ_1)| ≡ |F_0| ≡ 0 (mod 2)
Fix(ρ_1) ⊂ L (= F_0 \ {p, q})
Fix(ρ_1) = L ∩ S (where S = {y : y ~ σ y})
|Fix(τ) ∩ S| = |L ∩ S| (x_0, u_τ ∉ S)
⟹ |Fix(τ) ∩ S| ≡ 0 (mod 2)
```

### 進展サマリー (Steps 1-9 完了, 11 sorry 残)

* **Step 1** [DihedralC5Reflection.lean](../Moore57/Order22OnMoore57/Dihedral/DihedralC5Reflection.lean) ✅
  - `Fin5_cycle_involution_classify` (`native_decide`): D_5 element 全列挙で
    cycle-aut involution は id or reflection の二択.
  - K_{1,55} の no_leaf_leaf 性質で id 排除 (Fix(σ) ⊂ Fix(τ) 矛盾).
  - 唯一の τ-不動点 `u_τ` (= σ_fix.v idx) + cycle 隣接 a_τ, d_τ, b_τ, c_τ + swap 構造.

* **Step 2-4** [DihedralCenterInNeighborhood.lean](../Moore57/Order22OnMoore57/Dihedral/DihedralCenterInNeighborhood.lean) ✅
  - `dihedral_center_ne_uτ`: pigeonhole 矛盾.
    仮定 u_τ = center で 55 leaves が σ で N(u_τ)\leaves (2 元集合) に injection.
    補助補題 `σ² y = y ⟹ σ y = y` (∵ |σ| = 11 prime, σ^10 = (σ²)^5).
  - `dihedral_adj_uτ_center`: u_τ は K_{1,55} leaf, center と隣接.
  - `dihedral_center_not_σ_fixed`: Fix(σ)∩Fix(τ) = {u_τ} を経由.
  - `dihedral_σ_pow_adj_uτ_center`: N(u_τ) σ-stable から誘導.
  - b_τ, c_τ と center が非隣接 (μ=1, 共通近傍 = a_τ, d_τ ≠ center).

* **Step 5-8** [DihedralF0Structure.lean](../Moore57/Order22OnMoore57/Dihedral/DihedralF0Structure.lean) ✅
  - F_0 := branchFiber Γ u_τ x_0, F_1 := branchFiber Γ u_τ (σ x_0). |F_i| = 56.
  - σ(F_0) = F_1: `Finset.image_erase` + N(σ x) = σ N(x) helper.
  - F_0 τ-stable: τ x_0 = x_0, τ u_τ = u_τ.
  - p = unique vertex in N(b_τ) ∩ N(x_0), q = unique in N(c_τ) ∩ N(x_0).
    一意性補題 `dihedral_p_unique`, `dihedral_q_unique`.
  - p ≠ q: 三角形 b_τ-cτ-p で `no_triangle` 矛盾.
  - τ p = q (τ b_τ = c_τ, τ x_0 = x_0 で q の一意性に当てはめ).

* **Step 9** [DihedralLDefinition.lean](../Moore57/Order22OnMoore57/Dihedral/DihedralLDefinition.lean) ✅
  - L := F_0 \ {p, q}, |L| = 56 - 2 = 54.
  - Fix(τ|F_0) = (image of leaf) \ {u_τ} (54 leaves; u_τ 自身は leaf だが F_0 外).
  - Fix(τ|F_0) = L: card 同じ (54) + 包含で証明.

### 残 (Steps 10-15)

* **Step 10-14** [DihedralPiOneInvolution.lean](../Moore57/Order22OnMoore57/Dihedral/DihedralPiOneInvolution.lean)
  - m_1 : F_0 ≃ F_1 (`branchFiberMatchingEquiv`).
  - π_1 := σ⁻¹ ∘ m_1 : F_0 → F_0. y ∈ F_0 で `π_1 y = y ↔ y ~ σ y`.
  - τ ∘ π_1 = π_{-1} ∘ τ (dihedral relation).
  - π_{-1} = π_1⁻¹.
  - ρ_1 = τ ∘ π_1 is involution on F_0.

* **Step 15** [DihedralParity.lean](../Moore57/Order22OnMoore57/Dihedral/DihedralParity.lean)
  - p, q ∉ Fix(ρ_1) (4-cycle 排除).
  - Fix(ρ_1) ⊂ L.
  - |Fix(ρ_1)| ≡ 0 (mod 2).
  - 最終結合: |Fix(τ) ∩ S| ≡ 0 (mod 2).

### Build と axiom

Build 3668 jobs 成功 (sorry 11 個残).
現状 axiom: `propext, sorryAx, Classical.choice, Quot.sound`.
最終的に `Lean.ofReduceBool` (`native_decide` for Step 1 helper) も追加予定.
