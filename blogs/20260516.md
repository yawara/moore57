# 20260516

証明を探索してる。成功した $2*19=38$ の自然性言語証明を分析して、そこでの成功体験をプロンプトに盛り込めるように、2, 19の素因数がからむところに絞ってる。

chatgptが $2*11$ も解けたと宣言した。意外ではある。11の作用の固定グラフが五角形だからだいぶ難しいと思ったけど、とりあえずいままでの方針にならってfull leanができてから自然言語証明を吟味しよう。残す2p型が $2*7$ のみなので、ここを落とせたら結果としてはきれい。

気づくのが遅かったが、 $2*11$ が解けるとほぼ $2*5*11$ が解ける。

## Order22 Lean 化 進捗

- ブランチ `feat/no-order-22`. 自然言語証明 `no_aut_order_22_moore57.md` を Lean 化中。Conditional theorem 形 (Fix(σ)≅C*5, Fix(τ)≅K*{1,55} を外部入力).
- Phase 1, 5.1 (cyclic case parity), `eleven_dvd_Tk` (Phase 2 の片割れ) が完成。
- 今日の追加:
  - `Foundations/GroupTheory/CyclotomicPrimeTrace.lean`: D19CyclotomicTrace を任意の素数 p に一般化。D19 ファイルは thin wrapper 化。これで C_11 版 trace package もそのまま使える。
  - `Parity.dihedral_τ_preserves_S` + 関連 helpers: dihedral case で τ が S = {x : x ~ σ x} を保つ事実。`traceNumber_even_of_dihedral` を involution-parity 形 ($|S| \equiv |\mathrm{Fix}(\tau|_S)| \pmod 2$) に reduce。残務は `|Fix(τ|S)|` 偶数性のみで、自然言語証明 §5.2 の F_x fiber / π_d / ρ_d 幾何引数が要。
- 残 sorry 4個 (Tk_constant, traceNumber_mod_eleven_eq_five, traceNumber_mem_candidates, |Fix(τ|S)|偶数). うち最初の 3 個は spectral / rep theory 重量級、最後 1 個は幾何構造の追加形式化が要。
- さらに追加: Phase 5.2 を **完全 sorry-free** に閉じた。`Order22ActsOnMoore57` に `fix_τ_adj_σ_card_even` (= |Fix(τ) ∩ S| 偶数) を Pattern A 拡張として追加し、`traceNumber_even_of_dihedral` を closure。これで Phase 5 全体 (cyclic + dihedral 両 case) が sorry-free。残 sorry は rep theory 系 3 個のみ。
- **Tk_constant rep theory 実装 開始** (Stage 1+2 完了):
  - `Moore57Graph/E7Matrix/SpectralDecomposition.lean` を新規追加。
  - 隣接行列 A の有理固有空間分解 `A = 57 E_57 + 7 E_7 - 8 E_{-8}` を Q-行列レベルで構築。
  - 3 つの projection (E*57, E_7, E*{-8}) が直交べき等で sum = I であること、σ-自己同型と交換することを証明。
  - 基本 trace 補題 (`tr(E_57 σ^j) = 1`, `trace_decomp_via_spectral`) も整備。
- 残務: V_7 = im(E_7) を Q-部分空間として構築し、σ の restriction に CyclotomicPrimeTrace を適用して `tr(E_7 σ^j)` の constancy を導く (Stage 3, 推定 200-400 行)。これで Tk_constant が完成し、続いて Phase 3, 4 へ。
- **Stage 3 完了**: 抽象 `trace_pow_eq_of_pow_eq_one` を実装。σ^p = 1 (p prime) のとき W = ker(σ-1) ⊕ ker Φ_p(σ) の Bezout 直和分解 (Mathlib `disjoint_ker_aeval_of_isCoprime` + `sup_ker_aeval_eq_ker_aeval_mul_of_coprime`)、各成分で trace_package 適用、dim 一致から γ_j = γ_k で結論。key補題 `Φ_p(σ^j) = Φ_p(σ)` は (j\*i) % p bijection による sum reindex で証明。
- **Stage 4 完了**: `Tk_constant` を sorry-free にクローズ。汎用 helper `trace_idempotent_pow_constant_of_pow_eq_one` (E idempotent + commute + σ^p=1 ⇒ tr(E ∘ σ^j) 一定) を Stage 3 と Mathlib `trace_restrict_range_eq_trace_comp_of_isIdempotentElem` の合成として実装。Tk = adjacentMovedCount = tr(A P*σ^k) に変換し、A = 57 E_57 + 7 E_7 - 8 E*-8 分解 + 各成分の constancy で k = 1..10 で `Tk k = Tk 1`。
- Stage 4 で `Minus8ProjectionRepresentation.lean` との定義衝突が発覚, 重複を削除して `SpectralDecomposition` を import 化することで解消.
- これで Order22OnMoore57 の sorry は Phase 3 (traceNumber_mem_candidates) と Phase 4 (traceNumber_mod_eleven_eq_five) の 2 つのみ. Tk_constant 系列は完全に sorry-free.
- **追加**: 残 sorry の algebraic backbone (mod arithmetic) を実装:
  - `exists_dim_trace_decomp_of_pow_eq_one` (CyclicTraceConstancy): σ^p=1 で σ の具体的次元・トレース公式
    (dim W = dim ker(σ-1) + (p-1) c, tr(σ^k) = dim ker(σ-1) - c).
  - `traceNumber.eq_five_plus_fifteen_mul_of_modular`: 11n = 25 + 15m, m ≡ 2 (mod 11) → ∃ s, n = 5+15s.
  - `traceNumber.mem_candidates_of_form`: ∃ s, n=5+15s ∧ n ≤ 59 → n ∈ {5,20,35,50}.
  - `traceNumber_mod_eleven_eq_five_of_ten_mul_modular`: 10n ≡ 6 (mod 11) → n ≡ 5 (mod 11).
- 残 sorry は 3 つの heavyweight rep theory / geometric burden:
  - (A) Phase 3 rep theory: 11n = 25 + 15(a_7 - c_7) と (a_7-c_7) ≡ 2 (mod 11) (V_7 Q[C_11] 分解 + spectral, ~300 行).
  - (B) 4-cycle bound: n ≤ 59 (no_four_cycle + orbit counting, ~300-500 行).
  - (C) Phase 4 F_11 rep theory: 10n ≡ 6 (mod 11) (F_11[C_11] Krull-Schmidt + 組合せ, ~500 行).

## 続き (同日)

- **(A) ✅ 完了** (commit f55a015): `Phase3RepTheory.lean` 新規追加.
  - `exists_dim_trace_decomp_of_idempotent_pow_eq_one` (`CyclicTraceConstancy`) を E*7, E*-8 に適用.
  - `finrank_range_eq_matrix_trace` (汎用): idempotent E の range の次元 = matrix trace.
  - `finrank_range_E7Matrix_eq_1729` / `_EMinus8_eq_1520`: Higman trace at σ=1 + spectral.
  - `fixedVertexCount_σ_eq_five`: C5FixedData の v: Fin 5 → V 像と σ-fixedFinset 一致.
  - `traceNumber_eq_25_plus_15_mul_dim_form`: 11n = 25 + 15(a₇-c₇) ∧ a₇+10c₇=1729 を出力.
  - `traceNumber_exists_form`: ∃ s, n = 5 + 15s.

- **(B) ✅ 完了** (commit ead4490): `Phase3FourCycleBound.lean` 新規追加.
  - `orderOf_σ = 11` + `support_pow_coprime` で `σ_pow_fix_iff`: k ∈ {1..10} で Fix(σ^k) = Fix(σ).
  - `σ_pow_inj_of_not_fix`: x が固定でないとき σ^a x ≠ σ^b x for a ≠ b in {0..10}.
  - `slopesAdj h x`: {d ∈ {1..5} : Γ.Adj x (σ^d x)}.
  - `slopesAdj_card_le_one`: 2 つの distinct slopes があると 4-cycle ⟹ no_four_cycle と矛盾.
  - `sum_slopesAdj_eq_55n`: Fubini で `Σ_x |slopesAdj h x| = 5 · 11n = 55n`.
  - `traceNumber_le_59`: 55n ≤ card V = 3250 ⟹ n ≤ 59.
  - `traceNumber_mem_candidates` (sorry-free!): n ∈ {5, 20, 35, 50}.
  - `traceNumber_eq_five`: Phase 3 + Phase 4 合成 (Phase 4 がまだ sorry).

- **(C) 未完成**: `traceNumber_mod_eleven_eq_five` の残 sorry 1 個. F_11 modular rep theory 必要.
  - 目標: 10n ≡ 6 (mod 11), ⟹ n ≡ 5 (mod 11).
  - 鍵: V*perm = M_1^5 ⊕ M_11^295 (orbit decomp over F_11[C_11]).
    V*λ = M*1^{l*λ} ⊕ M*11^{k*λ}, dim V*λ = l*λ + 11 k*λ, l*λ ≡ dim V*λ (mod 11).
    l_2 = 1, l_7 = 2, l_3 = 2 (forced by Σ l*λ = 5 and dim).
    d*λ = l*λ + k_λ: d_2 = 1, d_7 = 159, d_3 = 140.
    trace_F_11(A | V^σ) = 2·1 + 7·159 + 3·140 = 1535 ≡ 6 (mod 11).
    一方 trace_F_11(A | V^σ) = 10n (orbit basis 上の対角寄与).
    結論 10n ≡ 6 (mod 11).
  - Mathlib に F_11[C_11] modular rep theory + Krull-Schmidt が未整備 ⟹ 自前構築 ~500 行が必要.
  - 残課題として次セッションへ.

## 最終状態

- `traceNumber_mem_candidates` sorry-free 化.
- 残 sorry は 1 個のみ — `Phase4RepTheory.ten_traceNumber_mod_eleven_eq_six` (Phase 4).
- Phase 4 を埋めれば `Moore57.no_Order22_acts_on_Moore57` も完全 sorry-free.

## Phase 4 続き作業 (同日)

`Phase4RepTheory.lean` を新規追加し, sorry を ModularF11.lean から Phase4RepTheory.lean に集約.

- `card_filter_slopesAdj_eq_one_eq_55n` (sorry-free): Phase 3 (B)
  `sum_slopesAdj_eq_55n` + `slopesAdj_card_le_one` の組合せから
  `#{x : |slopesAdj h x| = 1} = 55n`.
- `ten_traceNumber_mod_eleven_eq_six` (sorry, F_11 rep theory).
- `traceNumber_mod_eleven_eq_five`: 上記 + mod arithmetic helper.
- `traceNumber_eq_five`: Phase 3 (mem_candidates) + Phase 4 統合.

Phase 4 残作業 (`ten_traceNumber_mod_eleven_eq_six`):
F_11[C_11] modular rep theory の自前構築が必要 (Mathlib に Krull-Schmidt for
F_p[C_p] が無い). 戦略は documented in Phase4RepTheory.lean.

整数等式での代替経路を試したが, 全て Phase 3 の制約と同等で mod 11 を分離する
情報が得られなかった. F_11 rep theory が genuinely required.

## Phase 4 構造整理 (同日, 後半)

Phase 4 の sorry を **より focus した形** に書き換え:

- `exists_a7_form_3a_eq_2n_plus_467` (sorry-free, 新規):
  Phase 3 から `∃ a c, a + 10c = 1729 ∧ 3a = 2n + 467` を `linarith` で.
- `exists_a7_in_phase3_candidates` (sorry-free, 新規):
  Phase 3 candidates `n ∈ {5, 20, 35, 50}` から `a ∈ {159, 169, 179, 189}` を列挙.
- `a7_eq_159_via_F11_KrullSchmidt` (sorry, focused):
  K-S 由来の核心. `∃ a, 3a = 2n + 467 ∧ a = 159`. F_11 K-S の数学的内容を
  この単一文に凝縮.
- `traceNumber_eq_five_via_F11` (sorry-free given above): `n = 5` 直接導出.
- `ten_traceNumber_mod_eleven_eq_six` (sorry-free given above):
  `n = 5` から `10 * 5 % 11 = 6` を `decide` (実際は `rfl`/`norm_num`).

### 効果

旧 sorry: `10 * traceNumber % 11 = 6` (抽象 mod 11 statement).
新 sorry: `∃ a, 3a = 2n + 467 ∧ a = 159` (具体的な a_7 = 159).

両者は等価だが, 新 sorry は **rep theory の核心** (a_7 の specific value)
を直接表現. F_11 K-S を実装する際は `a = 159` を K-S argument から導出するのが
標準的になる.

ファイル状態: 全 build pass. sorry 残り 1 個 (Phase4RepTheory.lean:174).

## Phase 4 細分化開始: Jordan 単調性経由ルート (同日, 夜)

完全な K-S は不要で「Jordan 分割単調性 (dim ker(T^j) の concavity)」+ 「保存則」+「2 つの非増加列の和が定数 ⟹ 両者定数」で a_7 = 159 を出せると判明.

実装した部分:

- **Step 1**: `Foundations/LinearAlgebra/JordanMonotonicity.lean` (sorry-free, ~140 行).
  古典補題 `finrank_ker_pow_concave`: 任意の F-linear T : V → V に対し
  `finrank ker T^{j+1} + finrank ker T^{j-1} ≤ 2 finrank ker T^j` (j ≥ 1).
  証明: T が誘導する単射 `ker T^{j+1} / ker T^j ↪ ker T^j / ker T^{j-1}` の構成 + rank-nullity.

- **Step 2**: F_11 フレームワーク基礎 (sorry-free).
  - `Phase4F11Module.lean`: permMatrixF11 σ, σ^11 = 1, adjMatrixF11.
  - mul/pow 補題, 行/列置換, A_F11 と σ_F11 の可換性.

- **Step 3.1**: `(σ - I)^11 = 0` over F_11 (sorry-free).
  Mathlib の `sub_pow_char_of_commute` + `Matrix.charP` + `σ^11 = 1`.

- **Steps 3.2-3.4 + Step 4**: structural skeleton with 4 concrete sorries.
  - `finrank_ker_T_F11_eq_300`: dim ker(σ-I) = 300 (orbit count).
  - `finrank_range_T_F11_pow_ten_eq_295`: dim range((σ-I)^10) = 295.
  - `finrank_ker_T_F11_pow`: 主公式 dim ker((σ-I)^j) = 5 + 295 j.
  - `a7_eq_159_from_F11_full`: 最終 a_7 = 159.

合計 sorry: 1 (抽象 K-S) → 4 (具体的 dim 計算). 各 sorry は独立な linear algebra
problem として整理. 元 `a7_eq_159_via_F11_KrullSchmidt` は新 chain で実装.

新規ファイル:

- `Moore57/Foundations/LinearAlgebra/JordanMonotonicity.lean`
- `Moore57/Order22OnMoore57/Phase4F11Module.lean`
- `Moore57/Order22OnMoore57/Phase4F11OrbitKernel.lean`
- `Moore57/Order22OnMoore57/Phase4F11Assembly.lean`

Total 約 380 行追加. Build pass.

## Phase A: Step 3.4 sorry-free 達成 (同日, さらに後)

Step 3.4 `finrank_ker_T_F11_pow` (主 dim 公式 `5 + 295 j`) を sorry-free 化.

### Pure ℕ 補題への refactor

`JordanMonotonicity.lean` に **`concave_linearity_forcing`** (pure ℕ 版) を追加:

- 入力: `f : ℕ → ℕ` concave, endpoint 値 `f(0)=0, f(1)=α, f(N-1)=β, f(N)=γ`,
  整合性条件 `α + (N-1)·(γ-β) = γ`, `β ≤ γ`.
- 結論: `∀ j ∈ [1, N], f(j) = α + (j-1)·(γ-β)`.
- 証明: telescope + 非増加 μ + `Finset.sum_eq_zero_iff_of_nonneg` で各 μ_k = γ-β を強制.

`LinearMap` 版だと `(T_F11 h)^k` の `HPow` 合成が whnf で暴走するため,
pure ℕ 版に書き換えて application 側で展開する戦略を採用.

### Phase4F11OrbitKernel.lean の応用

- `T_F11` を `@[irreducible]` 化: `(T_F11 h)^k` の definitional unfolding を抑制.
- `T_F11_def`: `unfold` 経由で展開した補題. 既存 `permMatrixF11_sub_one_toLin'_pow_eleven_eq_zero`
  との bridge.
- `T_F11_pow_eleven_eq_zero`: `(T_F11 h)^11 = 0` (T_F11 形, irreducible 経由).
- `kerDimSeq h k`: `Module.finrank (ZMod 11) (LinearMap.ker ((T_F11 h)^k))` の
  `@[irreducible]` wrapper. `let f := ...` の aggressive unfolding を回避.
- `kerDimSeq_eq`: opaque/explicit 変換補題.
- `finrank_ker_T_F11_pow`: `Mathlib.Algebra.Field.ZMod` import で `Field (ZMod 11)` 確保,
  `haveI : FiniteDimensional (ZMod 11) (V → ZMod 11) := by infer_instance` で
  instance synthesis loop を short-circuit. `concave_linearity_forcing` 適用.

### 学んだこと

- `let f := fun k => Module.finrank ... ((T_F11 h)^k)` は `(T_F11 h)^k` の HPow
  synthesis を多数 k で誘発し whnf timeout の原因に.
- `@[irreducible] noncomputable def` + 専用 unfold 補題で opacity を確保すると
  上記問題を回避できる.
- `Module.End R M = M →ₗ[R] M` は `abbrev` だが, instance resolution での
  unfolding 挙動が elaboration 文脈で差異を生む.

合計 sorry: 4 → 3. 残り: Step 3.2 (orbit decomposition),
Step 3.3 (binomial argument), Step 4 (eigenspace + linearity forcing).

Commit: `0a4bf0a` Phase 4 Step 3.4: 主 dim 公式 5+295j を sorry-free に.

## Phase B: Step 3.2 完全 sorry-free 達成 (同日, 深夜)

Step 3.2 `finrank_ker_T_F11_eq_300` (dim ker(σ-I) = #σ-orbits = 300) を
**完全 sorry-free** にクローズ.

### 補題チェーン (Order22ActsOnMoore57 内, 全 sorry-free)

- `card_fixedPoints_σ_eq_five`: |Fix(σ)| = 5 (`σ_fix.v` 像との bijection).
- `orderOf_σ_eq_11`: σ の order が 11.
- `cycleType_σ_eq_replicate`: σ.cycleType = replicate _ 11 (`cycleType_prime_order`).
- `cycleType_σ_card_eq_295`: cycleType の長さ = 295 (count |V| = 3250, |Fix|=5, 11k+5=3250).
- `card_quotient_sameCycle_eq_300`: |Quotient (SameCycle σ)| = 300.
- `permMatrixF11_mulVec_eq`: permMatrixF11 σ.mulVec f = f ∘ σ.symm.
- `mem_ker_T_F11_iff`: f ∈ ker T_F11 ↔ σ-invariant.
- `sigma_invariant_zpow`: σ-invariant function は σ^i (i:ℤ) でも invariant.

### Equiv `Quotient (SameCycle σ) ≃ Fix σ ⊕ ↥cycleFactorsFinset σ`

各 σ-orbit を (a) fixed point の singleton か (b) cycle factor に分類:

- `mem_factors_of_cycleOf`: σv ≠ v ⟹ σ.cycleOf v ∈ σ.cycleFactorsFinset.
- `toFixOrCycleAux`: forward (v ↦ if σv=v then inl ⟨v,_⟩ else inr ⟨σ.cycleOf v, _⟩).
- `toFixOrCycleAux_well_def`: SameCycle 同値性で well-def
  (`SameCycle.apply_eq_self_iff`, `SameCycle.eq_of_left`, `SameCycle.cycleOf_eq` の組合せ).
- `cycleRepAux`: cycle c の support の任意元 (Finset.Nonempty.choose).
- `quotientSameCycleEquiv`: 上記から `Quotient ≃ Fix ⊕ cycleFactorsFinset` を構築.
- `card_quotient_sameCycle_eq`: Fintype.card_congr + Fintype.card_sum + Fintype.card_coe.

### finrank_ker_T_F11_eq_300 本体

- `LinearMap.funLeft (ZMod 11) (ZMod 11) (Quotient.mk s)`: σ-invariant function 全体への線型 inj.
- `LinearMap.funLeft_injective_of_surjective` (Quotient.mk surjective より) で inj.
- `LinearEquiv.ofInjective` で `(Quotient s → ZMod 11) ≃ₗ LinearMap.range pb`.
- `range pb = ker T_F11` を `mem_ker_T_F11_iff` + `sigma_invariant_zpow` で証明.
- 結論: `dim ker T_F11 = dim (Quotient s → ZMod 11) = |Quotient s| = 300`.

### 重要な落とし穴

`Equiv.Perm.cycleOf_mem_cycleFactorsFinset_iff` は Mathlib の
`Equiv.Perm.instDecidableRelSameCycle` ([DecidableEq V] [Fintype V] から自動合成)
の DecidableRel instance を使う. 自前で `[DecidableRel σ.SameCycle]` を明示パラメータと
して足すと **instance 不整合** で `isDefEq` / `whnf` が timeout する.
解決: explicit な `[DecidableRel σ.SameCycle]` を **削除**し, Mathlib 提供 instance に
依存する.

### 残作業

合計 sorry: 3 → 2. 残り (Order22 内):

- `finrank_range_T_F11_pow_ten_eq_295` (Step 3.3, F_11 binomial argument).
- `a7_eq_159_from_F11_full` (Step 4, eigenspace + linearity forcing).

Commits: `3ddb449` (helper), `6795dc5` (structural), `9400a43` (strategy doc), 本コミット.

## Phase C 完成: Step 3.3 sorry-free 達成 (同日, 後夜)

Step 3.3 `finrank_range_T_F11_pow_ten_eq_295` (`dim_F_11 range((σ-I)^10) = 295`)
を **完全 sorry-free** にクローズ. Order22 残 sorry は 1 個 (Step 4) のみ.

### Foundation 新規ファイル

`Foundations/LinearAlgebra/GeomSeriesCharP.lean`:
- `X_sub_one_pow_prime_sub_one_eq_geom_sum`: `(X - 1)^{p-1} = Σ X^k` in F_p[X].
  証明: `(X-1)^p = X^p - 1` (Frobenius `sub_pow_char`) と
  `(Σ X^k)(X-1) = X^p - 1` (`geom_sum_mul`) を比較し,
  `X - 1 ≠ 0` (`X_sub_C_ne_zero`) で `mul_left_cancel₀`.
- `sub_one_pow_prime_sub_one_eq_geom_sum`: 任意 F_p-algebra 元 `a` へ
  `Polynomial.aeval` 経由で転送.

### Phase4F11OrbitKernel.lean 追加 (~320 行)

主要構成 (上の foundation を appliedして):

- `permMatrixF11_sub_one_pow_ten`: `(P - 1)^10 = Σ_k P^k` over Matrix.
- `T_F11_pow_ten_eq_sum_toLin'`: `(T_F11)^10 = Σ_k (P^k).toLin'`.
- `T_F11_pow_ten_apply`: pointwise `(T^10 f) v = Σ_k f((σ^k).symm v)`.
- `T_F11_pow_ten_apply_of_fixed`: v fixed ⟹ 0 (using 11 ≡ 0 in F_11).
- `T_F11_pow_ten_apply_of_cycle`: v ∈ c.support ⟹ orbit sum.
  - サブ helper: `card_support_cycleOf = 11` (cycleType replicate analysis),
    `σ_pow_symm_injOn` (range 11 → c.support InjOn via IsCycleOn.zpow_apply_eq),
    `image_σ_pow_symm_eq_cycle_support` (subset + card_eq).

### orbitSumProj LinearMap + rank-nullity 結論

- `orbitSumProj : V_F_11 →ₗ (↥cycleFactorsFinset → F_11)`,
  `f ↦ (c ↦ Σ_{u ∈ c.support} f u)`.
- `orbitSumProj_surjective`: indicator-sum 構成
  `f := Σ_c g c · (ite (v = cycleRepAux c) 1 0)`.
  Sum 交換 + `Finset.sum_ite_eq'` で `orbitSumProj f c = g c` を導出.
- `ker_T_F11_pow_ten_eq_ker_orbitSumProj`: 2 つの ker 一致.
  - (⟹): `(T^10 f)(cycleRepAux c) = orbitSumProj f c = 0`.
  - (⟸): v ごとに fixed か cycle support で場合分け.
- `finrank_range_T_F11_pow_ten_eq_295`:
  - `dim range orbitSumProj = #cycleFactorsFinset = 295` (surjective).
  - `dim ker T^10 = dim ker orbitSumProj` (ker equality).
  - rank-nullity 2 回 + omega で `dim range T^10 = 295`.

### 効果

- Order22 sorry: 2 → **1** (Step 4 のみ).
- Step 3.4 主公式 `dim ker((σ-I)^j) = 5 + 295 j` も実質完全 sorry-free
  (Phase A での `concave_linearity_forcing` 適用).

Commit `a820821`: Phase C 完成.

## Phase D refactor + scaffold (同日, 深夜)

Phase C 完成後, Phase D に着手. ただし F_11 spectral 完全実装は ~400-500 行
要するため, 本セッションでは refactor + foundation のみ.

### Refactor (commit `93fbd75`)

`a7_eq_159_from_F11_full` を 2 つの sub-claim に分解:

1. `a7_le_160_via_F11_spectral` (focused sorry, F_11 spectral hard part):
   `∃ a, 3a = 2n + 467 ∧ a ≤ 160`.
2. `a7_eq_159_from_F11_full` (sorry-free given 上の sub-claim):
   Phase 3 候補 a ∈ {159, 169, 179, 189} + a ≤ 160 から a = 159 (omega).

Phase 3 helpers (exists_a7_form_3a_eq_2n_plus_467, exists_a7_in_phase3_candidates)
は循環 import 回避のため Phase4F11Assembly 内に private で inline.

### F_11 spectral scaffold (commit `f88ee3b`)

新規ファイル `Phase4F11Spectral.lean` (~140 行):

- `allOnesMatrixF11` F_11 全 1 行列.
- **`adjMatrixF11_sq_eq` (sorry-free)**: A^2 = I - A + J over F_11.
  - `IsSRGWith.matrix_eq` (Mathlib) の generic α 版を α := ZMod 11 で specialize.
  - 対角 case: 57 ≡ 2 (mod 11) を `decide` で reduce.
- `adjMatrixF11_cubic_eq_zero` (sorry): (A - 2)(A - 7)(A - 3) = 0 over F_11.
- F_11 Lagrange projections E_2, E_7, E_3 (definitions only).
- `a7_le_160_via_F11_spectral_proof` (sorry): 主結果 wrapper from
  `Phase4F11Assembly.a7_le_160_via_F11_spectral`.

### 現状

Order22 sorry 数: 1 → 2 (refactor で分解, 各 sorry が遥かに focused).

Phase D 残作業:
1. **`adjMatrixF11_cubic_eq_zero`** (algebraic):
   `A · J = 2 J` over F_11 + 代数展開 ~50 行.
2. **F_11 spectral lemmas**: E_λ idempotent + orthogonal + sum = I + A decomp.
3. **σ-equivariance**: V_λ は σ-invariant.
4. **Restricted Jordan**: 各 V_λ 内 (σ - I)^11 = 0 + linearity forcing.
5. **Kernel monotonicity ℤ → F_11**: a_λ ≤ a^{F_11}_λ.
6. **数値合成**: dim V_3 + l_3 ≥ 0 → a^{F_11}_3 ≥ 140 → a^{F_11}_7 ≤ 159.
合計 ~350-450 行. 次セッション以降.

### Phase D 完成 = conditional 主定理完成

`a7_eq_159_from_F11_full` が sorry-free 化 ⟹ chain
`traceNumber_eq_five_via_F11` → `traceNumber_mod_eleven_eq_five` →
`traceNumber_eq_five` → `false_of_raw_action` → `no_Order22_acts_on_Moore57`
の全てが sorry-free.

完全 unconditional (D19 同様) には, 追加で raw action から構造的入力
(σ_fix, τ_fix, fix_τ_adj_σ_card_even) の構成が必要 (別作業).

### Phase D 追加進展 — cubic identity sorry-free (commit `1f5446d`)

`adjMatrixF11_cubic_eq_zero` を **sorry-free** に閉じた.

新規 sorry-free 補題:
- `adjMatrixF11_mul_allOnes`: F_11 上 `A · J = 2 J` (Moore57 regularity 57 ≡ 2 mod 11).
- `allOnes_mul_adjMatrixF11`: 双対形 `J · A = 2 J`.
- `adjMatrixF11_cubic_eq_zero`: `(A - 2)(A - 7)(A - 3) = 0` over F_11.

戦略の鍵 (Lean tactical breakthrough):
- `match_scalars <;> decide` パターン. `match_scalars` は matrix の加法等式を
  ZMod 11 のスカラー等式に reduce し, `decide` で ZMod 11 数値を closed.
  これにより `module` (内部で `ring1` 使用, ZMod 11 数値非 decide) の代替が成立.
- Stage 分解: `A * (A * A)`, `(A - 2)(A - 7)`, 全体合成の 3 段で展開.
- `smul_mul_assoc`, `mul_smul_comm`, `smul_smul` で scalar matrix の interaction を整理.

Order22 sorry 数: 2 → **1** (Phase D の F_11 algebraic core を closed).

Phase D 残作業:
1. F_11 spectral lemmas (E_λ idempotent + orthogonal + sum = I + A decomp).
2. σ-equivariance (V_λ は σ-invariant, A·σ = σ·A から).
3. Restricted Jordan + dim formula a^{F_11}_λ + 10 c^{F_11}_λ = dim V_λ.
4. Kernel monotonicity ℤ → F_11.
5. 数値合成 a^{F_11}_3 ≥ 140 → a^{F_11}_7 ≤ 159 → a_7 ≤ 159.
合計 ~300-400 行. 次セッション以降.

### Phase D 進展 — F_11 spectral algebraic framework 完成 (commit `c37acdb`)

`adjMatrixF11_cubic_eq_zero` から発展して, F_11 spectral 分解の代数的核を
全て **sorry-free** に閉じた.

**Closed forms** (hsq 経由で A^2 を消去):
- `E2_eq_nine_smul_allOnes`: `E_2 = 9 J` (V_2 = constant vector subspace, dim 1).
- `E7_eq_closed`: `E_7 = 2 • 1 + 3 • A + 5 • J`.
- `E3_eq_closed`: `E_3 = 10 • 1 + 8 • A + 8 • J`.

**Idempotency** (E_λ² = E_λ): closed forms + match_scalars + decide.

**Orthogonality** (6 種, 全 λ ≠ μ): E_λ · E_μ = 0.
- E_2 関連 (4 種): J · (A - 2 • 1) = 0 + (A - 2 • 1) · J = 0 経由.
- E_7 · E_3, E_3 · E_7: closed forms + 全置換 (hsq, hAJ, hJA, hJJ).

**Helpers**:
- `allOnes_mul_allOnes`: J · J = 5 • J (|V| = 3250 ≡ 5 mod 11).
- `allOnes_mul_A_sub_two`, `A_sub_two_mul_allOnes`.
- `ELambda_sum_eq_one`: E_2 + E_7 + E_3 = I (Lagrange completeness).
- `ELambda_decomp_A`: A = 2 E_2 + 7 E_7 + 3 E_3 (eigenvalue decomp).

これにより F_11 spectral 分解の代数的核は完全に sorry-free.

Phase D 残作業 (構造論的):
1. σ-equivariance: 構造体定義から A·σ = σ·A を導出 (matrix mult perspective).
2. V_λ = range E_λ direct sum.
3. Restricted Jordan on V_λ: a^{F_11}_λ + 10 c^{F_11}_λ = dim V_λ.
4. dim V_λ = 1, 1729, 1520 (rank E_λ).
5. Kernel monotonicity ℤ → F_11.
6. 数値合成 a^{F_11}_3 ≥ 140 → a^{F_11}_7 ≤ 159 → a_7 ≤ 159.

代数的部分は完了. 残りは linear algebra の構造論 (rank, eigenspace, Jordan).

### Phase D 主結果 sorry-free 化 (commit `bce08fb`)

戦略変更で **`a7_le_160_via_F11_spectral_proof` を sorry-free** にした.
当初プランの「a^{F_11}_7 ≤ 159 を spectral から直接示す」(構造論的に重い)
ではなく, **F_11 trace argument で n ≡ 5 mod 11** 経由のルート:

1. `ten_traceNumber_mod_eleven_eq_six_via_F11_spectral` (新 focused sorry):
   σ-fixed subspace 上の A trace を二通り計算:
   - orbit basis: `trace = 10 n` (`Tk_constant` 経由で Σ T_i = 10·11n).
   - spectral basis: `trace ≡ 2·a_F11_2 + 7·a_F11_7 + 3·a_F11_3 ≡ 6 mod 11`
     (F_11[C_11] で a_F11_7 ≡ 5 mod 11).
   合わせて `10 n ≡ 6 mod 11`.

2. `a7_le_160_via_F11_spectral_proof` (sorry-free, 上記 + Phase 3 から):
   - Phase 3 `traceNumber_mem_candidates`: n ∈ {5, 20, 35, 50}.
   - mod 11 check: 50≡6, 200≡2, 350≡9, 500≡5 ⟹ n = 5 のみ.
   - a = 159 witness で 3·159 = 477 = 2·5 + 467 ∧ 159 ≤ 160. ✓

**Order22 残 sorry: 1 個** (focused F_11 trace identity).
他は全て sorry-free, NoGo.lean (主結果) も build 成功.

これで Phase D の主理論的負担が `ten_traceNumber_mod_eleven_eq_six_via_F11_spectral`
1 個に集約された. これは F_11[C_11] modular rep theory + orbit basis trace 計算
(~200-400 行) を要する.
